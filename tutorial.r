#help
help(solve)
?solve

#display names objects currently stored within the workspace
objects()
ls()

#remove objects from the workspace
rm(x, y, z)

#creating a vector by concatenating numbers
x <- c(10.4, 5.6, 3.1, 6.4, 21.7)
y <- c(x, 0, x)
